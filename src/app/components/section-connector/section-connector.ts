import { Component } from '@angular/core';

/** A small decorative dotted, gently curving line used to visually thread two similar sections together instead of leaving a flat seam between them. */
@Component({
  imports: [],
  selector: 'app-section-connector',
  styleUrl: './section-connector.scss',
  templateUrl: './section-connector.html',
})
export class SectionConnector {}
